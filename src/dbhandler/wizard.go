package dbhandler

import "github.com/NubeDev/flow-framework/model"

func (h *Handler) WizardNewNetDevPnt(plugin string, net *model.Network, dev *model.Device, pnt *model.Point) (bool, error) {
	/*TODO: Binod
	_, err := getDb().WizardNewNetDevPnt(plugin, net, dev, pnt)
	if err != nil {
		return false, err
	}*/
	return false, nil
}
